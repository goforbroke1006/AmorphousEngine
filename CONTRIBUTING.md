# Amorphous Engine

### How to build

Build the release

```shell
bash setup.sh
bash release.sh
```

and run the Battle City clone:

```shell
cd ./release
./launcher ./projects/02-BattleCity-Clone/
```
